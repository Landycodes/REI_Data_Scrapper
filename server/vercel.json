{
    "version": 2,
    "builds": [
      { "src": "server/*", "use": "@vercel/node" }
    ],
    "routes": [
      { "src": "/api/scrap", "dest": "server/server.js" },
      { "src": "/(.*)", "dest": "server/*" }
    ]
  }
  